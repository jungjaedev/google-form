import { Box } from './Box';
import { Structure } from './Structure';
import { TextField } from './TextField';

export const Default = {
  Box: Box,
  Structure: Structure,
  TextField: TextField,
};
